---

docker_ip: 192.168.1.55

docker_options:
  - "--privileged"

docker_volumes:
  - host_path: "{{ docker_root }}/{{ docker_name }}/config/exports"
    container_path: "/etc/exports"
  - host_path: "{{ docker_root }}/{{ docker_name }}/shares"
    container_path: "/srv/shares"
