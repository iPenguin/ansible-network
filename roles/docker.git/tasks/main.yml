---

- name: Include Docker skeleton role
  include_role:
    name: docker_skeleton
  tags:
    - build

- name: Create keys
  assemble:
    src: "files/keys/"
    dest: "{{ docker_root }}/{{ inventory_hostname }}/authorized_keys"
    remote_src: no
    owner: 1000
    group: 100
    mode: 0644
